# Pinned versions for P1 GeoData v2 reproducibility
# Generated: 2026-08-10
# Install: uv pip install -r requirements.txt

# --- Core geospatial ---
numpy>=2.0,<3.0
pandas>=2.2,<3.0
geopandas>=1.0,<2.0
shapely>=2.0,<3.0
fiona>=1.9,<2.0
pyproj>=3.6,<4.0
rasterio>=1.3,<2.0
geoalchemy2>=0.15
contextily>=1.5
folium>=0.16
mapclassify>=2.6

# --- Plotting ---
matplotlib>=3.8,<4.0
seaborn>=0.13
plotly>=5.20

# --- Statistics / ML base ---
scikit-learn>=1.5,<2.0
scipy>=1.13
statsmodels>=0.14

# --- Deep learning (CPU default; for GPU use --extra-index-url) ---
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.4,<3.0
torchvision>=0.19,<0.20
torchaudio>=2.4,<3.0

# --- Vision-language models ---
transformers>=4.45,<5.0
datasets>=2.20,<3.0
accelerate>=0.34,<1.0
peft>=0.10,<1.0
trl>=0.10,<1.0
bitsandbytes>=0.43,<1.0
open-clip-torch>=2.20,<3.0
ultralytics>=8.2,<9.0
supervision>=0.20
fiftyone>=1.0

# --- Sentence embeddings + LangChain ---
sentence-transformers>=3.0,<4.0
langchain>=0.3,<1.0
langchain-community>=0.3
langchain-huggingface>=0.1

# --- Vector DB ---
chromadb>=0.5,<1.0
faiss-cpu>=1.8

# --- Image processing ---
Pillow>=10.0,<11.0
opencv-python>=4.10,<5.0

# --- Notebooks ---
jupyter>=1.0
ipykernel>=6.0
ipywidgets>=8.0

# --- Development ---
pytest>=8.0
pytest-cov>=5.0
black>=24.0
ruff>=0.5
mypy>=1.10

# --- Annotation tool ---
label-studio>=1.8

# --- Schema validation ---
jsonschema>=4.20

# --- Cloud SDKs ---
huggingface_hub[cli]>=0.24
gsutil>=5.30

# --- HTTP for downloading data ---
requests>=2.32
tqdm>=4.66